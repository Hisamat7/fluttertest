import "package:flutter/material.dart";

class SingleLineLayout extends StatelessWidget {
 // const SingleLineLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.all(100),
      child:  Text("aju developer"),
    );
  }
}
